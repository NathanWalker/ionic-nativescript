import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.nstudio.ngconf2021',
  appName: 'ngconf',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
